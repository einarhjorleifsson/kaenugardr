.Rproj.user
.Rhistory
.RData
.Ruserdata
.positai
*.html
*.Rproj
.DS_Store

# Build outputs — regenerated by ports_all.R / ports-osm.R, never committed
ports_all.gpkg
ports_dedup.gpkg
ports_osm.fgb

# Parquet is ignored by default, but these two are INPUTS the build needs
*.parquet
!unlocode.parquet
!ports_kvoti.parquet
